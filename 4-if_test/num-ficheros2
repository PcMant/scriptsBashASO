numfi=`ls -l | wc -l`
echo "El número de fichero es: $numfi"
if test "$numfi" -gt "10"
then
	echo "Hay más de 10 ficheros"
else
	if test "$numfi" -lt "10"
	then
		echo "Hay menos de 10 ficheros"
	fi
fi
